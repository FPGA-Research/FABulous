"""Placement-driven optimisation of the FABulous GDS flow.

`placement` is the geometry a placed tile presents to the
optimisations that read it: `config_mapping` moves each configuration bit to
the frame crosspoint nearest its latch.
"""
